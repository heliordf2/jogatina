DATABASE_URL=postgresql://user:password@host/database?sslmode=require
PORT=3001

# Opcional — só para API em outro domínio. NÃO use localhost/127.0.0.1 na Vercel.
# VITE_API_URL=https://sua-api.exemplo.com/api
